: Int <: Int
> ok

: Int <: Bool
> _:0: The tag should be Bool, but tag Int is present.
> 
>      found: Int
>   expected: Bool
> This Int comes from test:1:
>   : Int <: Bool
>     ^^^
> but is used as Bool at test:1:
>   : Int <: Bool
>            ^^^^

: [a] (a) -> a <: (Int) -> Int
> ok

: (Int) -> Int <: [a] (a) -> a
> _:0: Type error
> 
>      found: Int -> _
>   expected: [a] a -> _
> This a comes from test:1:
>   : (Int) -> Int <: [a] (a) -> a
>                          ^
> but is used as Int at test:1:
>   : (Int) -> Int <: [a] (a) -> a
>      ^^^

: [a, b] (a, b) -> (a|b) <: [x] (x, x) -> x
> ok

: [x] (x, x) -> x <: [a, b] (a, b) -> (a|b)
> ok

: [x] () -> {foo: (x) -> x, bar: (x) -> x} <: () -> {foo: (Int) -> Int, bar: (Int) -> Int}
> ok

: [x] () -> {foo: (x) -> x, bar: (x) -> x} <: [x <: Int] () -> {foo: (x) -> Int, bar: (x) -> x}
> ok

: [x] () -> {foo: (x) -> x, bar: (x) -> x} <: () -> {foo: (Int) -> Int, bar: (String) -> String}
> _:0: The tag should be Int, but tag String is present.
> 
>      found: () -> {foo: _, bar: Int -> _}
>   expected: () -> {foo: _, bar: String -> _}
> This String comes from test:1:
>   : [x] () -> {foo: (x) -> x, bar: (x) -> x} <: () -> {foo: (Int) -> Int, bar: (String) -> String}
>                                                                                 ^^^^^^
> but is used as Int at test:1:
>   : [x] () -> {foo: (x) -> x, bar: (x) -> x} <: () -> {foo: (Int) -> Int, bar: (String) -> String}
>                                                                      ^^^

: [x] () -> {foo: (x) -> x, bar: (x) -> x} <: [x, y] () -> {foo: (x) -> x, bar: (y) -> y}
> _:0: Type error
> 
>      found: () -> {foo: _, bar: (_) -> _}
>   expected: [x, y] () -> {foo: _, bar: y -> _}
> This y comes from test:1:
>   : [x] () -> {foo: (x) -> x, bar: (x) -> x} <: [x, y] () -> {foo: (x) -> x, bar: (y) -> y}
>                                                                                    ^
> but is used as x at test:1:
>   : [x] () -> {foo: (x) -> x, bar: (x) -> x} <: [x, y] () -> {foo: (x) -> x, bar: (y) -> y}
>                                                                           ^

: [x, y] () -> {foo: (x) -> x, bar: (y) -> y} <: [x] () -> {foo: (x) -> x, bar: (x) -> x}
> ok

# all combinations of Int -> Int w/ one rigid var

: [x <: Int] (x) -> (x|Int) <: (Int) -> Int
> ok
: (Int) -> Int <: [x <: Int] (x) -> (x|Int)
> ok

: [x <: Int] (x) -> x <: (Int) -> Int
> ok
: (Int) -> Int <: [x <: Int] (x) -> x
> _:0: Type error
> 
>      found: (_) -> Int
>   expected: [x <: _] (_) -> x
> This Int comes from test:1:
>   : (Int) -> Int <: [x <: Int] (x) -> x
>              ^^^
> but is used as x at test:1:
>   : (Int) -> Int <: [x <: Int] (x) -> x
>                                       ^

: [x] (x) -> (x|Int) <: (Int) -> Int
> ok
: (Int) -> Int <: [x] (x) -> (x|Int)
> _:0: Type error
> 
>      found: Int -> _
>   expected: [x] x -> _
> This x comes from test:1:
>   : (Int) -> Int <: [x] (x) -> (x|Int)
>                          ^
> but is used as Int at test:1:
>   : (Int) -> Int <: [x] (x) -> (x|Int)
>      ^^^

: [x] (x) -> (x) <: (Int) -> Int
> ok
: (Int) -> Int <: [x] (x) -> (x)
> _:0: Type error
> 
>      found: Int -> _
>   expected: [x] x -> _
> This x comes from test:1:
>   : (Int) -> Int <: [x] (x) -> (x)
>                          ^
> but is used as Int at test:1:
>   : (Int) -> Int <: [x] (x) -> (x)
>      ^^^



: [x <: Int] (x) -> (x|Bool) <: (Int) -> Bool
> _:0: Type error
> 
>      found: (_) -> Int
>   expected: (_) -> Nothing
> This Int comes from test:1:
>   : [x <: Int] (x) -> (x|Bool) <: (Int) -> Bool
>                                    ^^^
> but is used as Nothing at test:1:
>   : [x <: Int] (x) -> (x|Bool) <: (Int) -> Bool
>           ^^^
: (Int) -> Bool <: [x <: Int] (x) -> (x|Bool)
> ok




: [x <: Int, y] (x) -> (y|Int) <: (Int) -> Int
> ok

: (Int) -> Int <: [x <: Int, y] (x) -> (y|Int)
> ok

: (Int) -> Int <: [] (Int) -> Int
> ok

: [a <: Int] (a) -> (Int, ([b] (a) -> a)) <: (Int) -> (Int, [a <: Int] (a) -> a)
> _:0: Type error
> 
>      found: (_) -> (_, (_) -> Int)
>   expected: (_) -> (_, [a <: _] (_) -> Nothing)
> This Int comes from test:1:
>   : [a <: Int] (a) -> (Int, ([b] (a) -> a)) <: (Int) -> (Int, [a <: Int] (a) -> a)
>                                                 ^^^
> but is used as Nothing at test:1:
>   : [a <: Int] (a) -> (Int, ([b] (a) -> a)) <: (Int) -> (Int, [a <: Int] (a) -> a)
>           ^^^

: (Int) -> (Int, [a <: Int] (a) -> a) <: [a <: Int] (a) -> (Int, ([b] (a) -> a))
> ok

: [b <: Int] (b) -> b <: [a <: Int] (a) -> a
> ok

: [b <: Int] (b) -> b <: [a <: Int, c] (a) -> a
> ok

# Mitchell's distributivity rule, hoisted version
# (Original is not true here, because we don't hoist foralls out of function results)
: [a] ((a) -> a) -> ((a) -> a) <: [c] ([b] (b) -> b) -> ((c) -> c)
> ok

: [c] ([b] (b) -> b) -> ((c) -> c) <: [a] ((a) -> a) -> ((a) -> a)
> _:0: Type error
> 
>      found: ([b] b -> _) -> _
>   expected: [a] (a -> _) -> _
> This b comes from test:1:
>   : [c] ([b] (b) -> b) -> ((c) -> c) <: [a] ((a) -> a) -> ((a) -> a)
>               ^
> but is used as a at test:1:
>   : [c] ([b] (b) -> b) -> ((c) -> c) <: [a] ((a) -> a) -> ((a) -> a)
>                                               ^

# same, but with name collision
: [c] ([a] (a) -> a) -> ((c) -> c) <: [a] ((a) -> a) -> ((a) -> a)
> _:0: Type error
> 
>      found: ([a] a -> _) -> _
>   expected: [a_2] (a_2 -> _) -> _
> This a comes from test:1:
>   : [c] ([a] (a) -> a) -> ((c) -> c) <: [a] ((a) -> a) -> ((a) -> a)
>               ^
> but is used as a_2 at test:1:
>   : [c] ([a] (a) -> a) -> ((c) -> c) <: [a] ((a) -> a) -> ((a) -> a)
>                                               ^



# scope escape check
: [a] ((a) -> a) -> ((a) -> a) <: ([b](b) -> b) -> ([c](c) -> c)
> _:0: Type error
> 
>      found: (_) -> (_) -> Any
>   expected: (_) -> [c] (_) -> Nothing
> This Any comes from test:1:
>   : [a] ((a) -> a) -> ((a) -> a) <: ([b](b) -> b) -> ([c](c) -> c)
>                                                           ^
> but is used as Nothing at test:1:
>   : [a] ((a) -> a) -> ((a) -> a) <: ([b](b) -> b) -> ([c](c) -> c)
>                                                                 ^


# tricky case of spec sub & rigvars (see cnrig <> [] case in ensure_upper_matches)
: [a <: {cond: Bool}] (a) -> {foo: Int, cond: Int} | a <: [a <: {cond: Bool}] (a) -> {foo: Int, cond: Int} | a
> ok

: [a <: {cond: Bool}] a <: [a <: {cond: String}] ({cond: Int} | a)
> ok

: [b <: {cond: Any}] b <: [a <: {cond: Bool}] ({cond: Int} | a)
> ok

: [a <: {cond: Bool}] ((a) -> ({cond: Int} | a)) <: [a <: {cond: Bool}] ((a) -> ({cond: Int} | a))
> ok

# found by fuzzing!
: [d, a] {bar: {p: a} | a, foo: {p: d} | a} <: 
  [d, a] {bar: {p: a} | a, foo: {p: d} | a}
> ok

: [d <: {foo: {foo: Int}}, a] ({foo: a | {foo: Int}} | a | d) <:
  [d <: {foo: {foo: Int}}, a] ({foo: a | {foo: Int}} | a | d)
> ok

: [a] ({foo: a | {foo: Int}} | a) <:
  [d <: {foo: {foo: Int}}] ({foo: {foo: Int}} | d)
> ok

: [a] (a | {foo:Int, bar:a}) <: [a] (a | {foo:Int, bar:Int})
> ok


: [c] (c | {bar: c}) <: [c] {bar: c}
> ok
: [c] {bar: c} <: [c <: {bar: Bool}] {bar: c}
> ok
: [c] (c | {bar: c}) <: [c <: {bar: Bool}] {bar: c}
> ok

# Tricky case showing that match_sub must refreshen matchable
# variables when the set of rigvars in the bound increases
:  [p <: {foo: Any}](p) -> p
<: [p <: {foo: Any}](p) -> {foo:Int} | p
> ok

> STATS: fix: 0, flex: 81
