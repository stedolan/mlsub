
fn(x) { (#Foo(), #Bar, #Foo{x: 42}, #Baz(1)) }
> fn (x) { (#Foo, #Bar, #Foo{x: 42}, #Baz(1)) }
>   fn (x: Any) -> (#Foo, #Bar, #Foo{x: Int}, #Baz(Int)) {
>     (#Foo, #Bar, #Foo{x: 42}, #Baz(1))
>   }
> Any -> (#Foo, #Bar, #Foo{x: Int}, #Baz(Int))
> let  = %yield(fun (x) ret {
>   ret((0: Foo(), 1: Bar(), 2: Foo(x: 42), 3: Baz(0: 1)))
> });
> done

# Checking mode
( if true { #Foo(@true) } else { #Bar(6) }
  : #Foo(Bool) | #Bar(Int) )
> (if true { #Foo(@true) } else { #Bar(6) } : #Foo(Bool) | #Bar(Int))
>   (if true { #Foo(@true) } else { #Bar(6) } : #Foo(Bool) | #Bar(Int))
> #Foo(Bool) | #Bar(Int)
> let k(x) = {
>   let  = %yield(x);
>   done
> };
> match <true> {
>   true -> {
>     k(Foo(0: <true>))
>   };
>   false -> {
>     k(Bar(0: 6))
>   }
> }

(#{x: 42} : #Foo{x: Int})
> (#{x: 42} : #Foo{x: Int})
> test:1: The tag should be #Foo, but tag # is present.
>   (#{x: 42} : #Foo{x: Int})
>    ^^^^^^^^
>      found: #{x: _}
>   expected: #Foo{x: _}
> This #{x: _} is used as #Foo{x: _} at test:1:
>   (#{x: 42} : #Foo{x: Int})
>               ^^^^^^^^^^^^

(#Bar{x: 42} : #Foo{x: Int})
> (#Bar{x: 42} : #Foo{x: Int})
> test:1: The tag should be #Foo, but tag #Bar is present.
>   (#Bar{x: 42} : #Foo{x: Int})
>    ^^^^^^^^^^^
>      found: #Bar{x: _}
>   expected: #Foo{x: _}
> This #Bar{x: _} is used as #Foo{x: _} at test:1:
>   (#Bar{x: 42} : #Foo{x: Int})
>                  ^^^^^^^^^^^^

#
# Matching syntax in functions and lets
#
fn(x, x) { x }
> fn (x, x) { x }
> test:1: The variable name x is already in use:
>   fn(x, x) { x }
>         ^
> as it is previously bound here:
>   fn(x, x) { x }
>      ^

fn((#A|#B), x) { x }
> fn ((#A | #B), x) { x }
>   fn [a]((#A | #B): #A | #B, x: a) -> a { x }
> [a] (#A | #B, a) -> a
> let  = %yield(fun (param, x) ret { ret(x) }); done

fn(x) { let (a, b, c) = x; (c, b, a) }
> fn (x) { let (a, b, c) = x; (c, b, a) }
>   fn [a, b, c](x: (a, b, c)) -> (c, b, a) {
>     let (a, b, c) : (a, b, c) = x;
>     (c, b, a)
>   }
> [a, b, c] (a, b, c) -> (c, b, a)
> let  = %yield(fun (x) ret {
>   let a = x.0;
>   let b = x.1;
>   let c = x.2;
>   ret((0: c, 1: b, 2: a))
> });
> done

#
# Match errors
#

fn(x) { match x { (_,_) => 1 | (_,_,_) => 2 } }
> fn (x) { match x { (_, _) => 1 | (_, _, _) => 2 } }
> test:1: This pattern:
>   fn(x) { match x { (_,_) => 1 | (_,_,_) => 2 } }
>                     ^^^^^
> is incompatible with the pattern at test:1:
>   fn(x) { match x { (_,_) => 1 | (_,_,_) => 2 } }
>                                  ^^^^^^^

fn(x) { match x { (_,_,_) => 1 | (_,_) => 2 } }
> fn (x) { match x { (_, _, _) => 1 | (_, _) => 2 } }
> test:1: This pattern:
>   fn(x) { match x { (_,_,_) => 1 | (_,_) => 2 } }
>                     ^^^^^^^
> is incompatible with the pattern at test:1:
>   fn(x) { match x { (_,_,_) => 1 | (_,_) => 2 } }
>                                    ^^^^^

fn(x) { match x { #{p,q: qq} => 1 | #{p,r} => 2 } }
> fn (x) { match x { #{p, q: qq} => 1 | #{p, r} => 2 } }
> test:1: This pattern:
>   fn(x) { match x { #{p,q: qq} => 1 | #{p,r} => 2 } }
>                     ^^^^^^^^^^
> is incompatible with the pattern at test:1:
>   fn(x) { match x { #{p,q: qq} => 1 | #{p,r} => 2 } }
>                                           ^

fn(x) { match x { #{p,r} => 1 | #{p,q: qq} => 2 } }
> fn (x) { match x { #{p, r} => 1 | #{p, q: qq} => 2 } }
> test:1: This pattern:
>   fn(x) { match x { #{p,r} => 1 | #{p,q: qq} => 2 } }
>                         ^
> is incompatible with the pattern at test:1:
>   fn(x) { match x { #{p,r} => 1 | #{p,q: qq} => 2 } }
>                                   ^^^^^^^^^^


fn(x) { match x { (a,a) => 1 } }
> fn (x) { match x { (a, a) => 1 } }
> test:1: The variable name a is already in use:
>   fn(x) { match x { (a,a) => 1 } }
>                        ^
> as it is previously bound here:
>   fn(x) { match x { (a,a) => 1 } }
>                      ^

fn(x) { match x, x { a, b, c => 2 } }
> fn (x) { match x, x { a, b, c => 2 } }
> test:1: Expected 2 patterns but found 3:
>   fn(x) { match x, x { a, b, c => 2 } }
>                        ^^^^^^^

fn(x) { match x { #Foo => 1 | _ => 2 } }
> fn (x) { match x { #Foo => 1 | _ => 2 } }
> test:1: Cannot determine which cases this pattern matches
>   fn(x) { match x { #Foo => 1 | _ => 2 } }
>                                 ^

fn(x) { match x { (a | #Foo) => 1 } }
> fn (x) { match x { (a | #Foo) => 1 } }
> test:1: The variable a must be bound on both sides of this or-pattern
>   fn(x) { match x { (a | #Foo) => 1 } }
>                     ^^^^^^^^^^

fn(x) { match (#A(1, 2)) { #A(x,#C) => 1 } }
> fn (x) { match #A(1, 2) { #A(x, #C) => 1 } }
> test:1: This pattern is unused
>   fn(x) { match (#A(1, 2)) { #A(x,#C) => 1 } }
>                              ^^^^^^^^
> test:1: Some cases of this pattern-match are missing:
>   fn(x) { match (#A(1, 2)) { #A(x,#C) => 1 } }
>                 ^^^^^^^^^^
> The following cases are unhandled:
> | _
>   fn (x: Any) -> Int { match #A(1, 2) { #A(x, #C) => 1 } }
> Any -> Int
> let  = %yield(fun (x) ret { let x2 = A(0: 1, 1: 2).0; match failure }); done

fn(x : #A(Int, Int) | #B(Int, Int)) {
  match x {
  | #A(x, y) => (x, y)
  | (a, b) => (a, b)
  }
}
> fn (x: #A(Int, Int) | #B(Int, Int)) {
>   match x {
>     | #A(x, y) => (x, y)
>     | (a, b) => (a, b)
>   }
> }
> test:4: This pattern is unused
>     | (a, b) => (a, b)
>       ^^^^^^
> test:2: Some cases of this pattern-match are missing:
>     match x {
>           ^
> The following cases are unhandled:
> | #B(_, _)
>   fn (x: #A(Int, Int) | #B(Int, Int)) -> (Int, Int) {
>     match x {
>       | #A(x, y) => (x, y)
>       | (a, b) => (a, b)
>     }
>   }
> (#A(Int, Int) | #B(Int, Int)) -> (Int, Int)
> let  = %yield(fun (x) ret {
>   match x {
>     A -> {
>       let x2 = x.0;
>       let y = x.1;
>       ret((0: x2, 1: y))
>     };
>     B -> {
>       match failure
>     }
>   }
> });
> done


fn(c) {
  let a = if c {#Foo{x:#Foo}} else {#Bar{x:#Bar}};
  (a, a.x)
}
> fn (c) { let a = if c { #Foo{x: #Foo} } else { #Bar{x: #Bar} }; (a, a.x) }
>   fn (c: Bool) -> (#Foo{x: #Foo} | #Bar{x: #Bar}, #Foo | #Bar) {
>     let a : #Foo{x: #Foo} | #Bar{x: #Bar} =
>     if c { #Foo{x: #Foo} } else { #Bar{x: #Bar} };
>     (a, a.x)
>   }
> Bool -> (#Foo{x: #Foo} | #Bar{x: #Bar}, #Foo | #Bar)
> let  = %yield(fun (c) ret {
>   let k(x) = {
>     ret((0: x, 1: x.x))
>   };
>   match c {
>     true -> {
>       k(Foo(x: Foo()))
>     };
>     false -> {
>       k(Bar(x: Bar()))
>     }
>   }
> });
> done

fn(x) {
  match x {
  | #A(_, b) => b
  | #B{foo: (#C|#D), bar:x} => x
  }
}
> fn (x) { match x { #A(_, b) => b | #B{foo: (#C | #D), bar: x} => x } }
>   fn [a, b](x: #A(Any, a) | #B{foo: #C | #D, bar: b}) -> (a | b) {
>     match x {
>       | #A(_, b) => b
>       | #B{foo: (#C | #D), bar: x} => x
>     }
>   }
> [a, b] (#A(Any, a) | #B{foo: #C | #D, bar: b}) -> a | b
> let  = %yield(fun (x) ret {
>   match x {
>     A -> {
>       let b = x.1;
>       ret(b)
>     };
>     B -> {
>       let x2 = x.bar;
>       ret(x2)
>     }
>   }
> });
> done


fn(x) {
  match x {
  | #A(_, b) => b
  | #A(_, _, _) => 42
  | #B{foo: (#C|#D), bar:x} => x
  }
}
> fn (x) {
>   match x {
>     | #A(_, b) => b
>     | #A(_, _, _) => 42
>     | #B{foo: (#C | #D), bar: x} => x
>   }
> }
> test:3: This pattern:
>     | #A(_, b) => b
>       ^^^^^^^^
> is incompatible with the pattern at test:4:
>     | #A(_, _, _) => 42
>       ^^^^^^^^^^^

#
# Regression for a reversal bug
#

fn(a, b) {
  match a, b {
  | #A, y => y
  | #B, y => y
  }
}
> fn (a, b) { match a, b { #A, y => y | #B, y => y } }
>   fn [a](a: #A | #B, b: a) -> a { match a, b { #A, y => y | #B, y => y } }
> [a] (#A | #B, a) -> a
> let  = %yield(fun (a, b) ret { match a { A -> { ret(b) }; B -> { ret(b) } } });
> done


fn() {
  match #D{x:5} {
    #D{x, y} => (x, y)
  }
}
> fn () { match #D{x: 5} { #D{x, y} => (x, y) } }
> test:3: The field 'y' is missing.
>       #D{x, y} => (x, y)
>       ^^^^^^^^
>      found: #D{x: _}
>   expected: #D{x: _, y: _}
> This #D{x: _} comes from test:2:
>     match #D{x:5} {
>           ^^^^^^^
> but is used as #D{x: _, y: _} at test:3:
>       #D{x, y} => (x, y)
>             ^


#
# At-patterns
#

fn((a @ b @ c @ (d,e), f)){(a,b,c,d,e,f)}
> fn (((a @ b @ c @ (d, e)), f)) { (a, b, c, d, e, f) }
>   fn [a, b, c <: (a, b), d](((a @ b @ c @ (d, e)), f): (c, d)) ->
>     (c, c, c, a, b, d) {
>     (a, b, c, d, e, f)
>   }
> [a, b, c <: (a, b), d] (c, d) -> (c, c, c, a, b, d)
> let  = %yield(fun (param) ret {
>   let a = param.0;
>   let d = param.0.0;
>   let e = param.0.1;
>   let f = param.1;
>   ret((0: a, 1: a, 2: a, 3: d, 4: e, 5: f))
> });
> done

fn(x) { match x { (z @ (#A | #B)) => z } }
> fn (x) { match x { (z @ #A | #B) => z } }
>   fn [a <: #A | #B](x: a) -> a { match x { (z @ #A | #B) => z } }
> [a <: #A | #B] a -> a
> let  = %yield(fun (x) ret { ret(x) }); done

fn(p, q) {
  match p, q {
    | (y @ #A), (z @ #A)
    | (z @ #A), (y @ #B)
    | (y @ #B), (z @ #A) => (y,z)
    | (z @ #B), (y @ #B) => (y,z)
  }
}
> fn (p, q) {
>   match p, q {
>     | (y @ #A), (z @ #A)
>     | (z @ #A), (y @ #B)
>     | (y @ #B), (z @ #A) => (y, z)
>     | (z @ #B), (y @ #B) => (y, z)
>   }
> }
>   fn [a <: #A | #B, b <: #A | #B](p: a, q: b) -> (a | b, a | b) {
>     match p, q {
>       | (y @ #A), (z @ #A)
>       | (z @ #A), (y @ #B)
>       | (y @ #B), (z @ #A) => (y, z)
>       | (z @ #B), (y @ #B) => (y, z)
>     }
>   }
> [a <: #A | #B, b <: #A | #B] (a, b) -> (a | b, a | b)
> let  = %yield(fun (p, q) ret {
>   let case(y, z) = {
>     ret((0: y, 1: z))
>   };
>   match p {
>     A -> {
>       match q {
>         A -> {
>           case(p, q)
>         };
>         B -> {
>           case(q, p)
>         }
>       }
>     };
>     B -> {
>       match q {
>         A -> {
>           case(p, q)
>         };
>         B -> {
>           ret((0: q, 1: p))
>         }
>       }
>     }
>   }
> });
> done


# Grammar ambiguity requires parens on return type
# (otherwise function body is misread as fields)
fn(x : #A | #B | #C(Int) | #D{x:Int}) -> Int {
  match x { #A => 1 | #B() => 2 | #C(x) => 3 | #D{x:foo} | #E(foo,_) => 5 }
}
> fn (x: #A | #B | #C(Int) | #D{x: Int}) -> Int {
>   match x {
>     | #A => 1
>     | #B => 2
>     | #C(x) => 3
>     | #D{x: foo}
>     | #E(foo, _) => 5
>   }
> }
>   fn (x: #A | #B | #C(Int) | #D{x: Int}) -> Int {
>     match x {
>       | #A => 1
>       | #B => 2
>       | #C(x) => 3
>       | #D{x: foo}
>       | #E(foo, _) => 5
>     }
>   }
> (#A | #B | #C(Int) | #D{x: Int}) -> Int
> let  = %yield(fun (x) ret {
>   match x {
>     A -> {
>       ret(1)
>     };
>     B -> {
>       ret(2)
>     };
>     C -> {
>       let x2 = x.0;
>       ret(3)
>     };
>     D -> {
>       let foo = x.x;
>       ret(5)
>     }
>   }
> });
> done

# FIXME the exhaustivity checking is kinda broken here as types change
# Should either report an unused pat on case 2 or unhandled (B,B)
fn (x) {
  match x {
  | (#A, #A) => 1
  | (#A, #B) => 2
  | (#B, #A) => 3
  }
}
> fn (x) { match x { (#A, #A) => 1 | (#A, #B) => 2 | (#B, #A) => 3 } }
>   fn (x: (#A | #B, #A)) -> Int {
>     match x {
>       | (#A, #A) => 1
>       | (#A, #B) => 2
>       | (#B, #A) => 3
>     }
>   }
> (#A | #B, #A) -> Int
> let  = %yield(fun (x) ret {
>   match x.0 {
>     A -> {
>       match x.1 {
>         A -> {
>           ret(1)
>         };
>         B -> {
>           ret(2)
>         }
>       }
>     };
>     B -> {
>       ret(3)
>     }
>   }
> });
> done


let make = fn (x) {
  if x.good {
    #Good{foo: x.foo, bar: x.bar}
  } else {
    #Bad{foo: x.foo}
  }
};
fn (x) {
  let y = make(x);
  (y.foo, y.bar)
}
> let make =
> fn (x) { if x.good { #Good{foo: x.foo, bar: x.bar} } else { #Bad{foo: x.foo} } };
> fn (x) {
>   let y = make(x);
>   (y.foo, y.bar)
> }
> test:10: The field 'bar' is missing.
>     (y.foo, y.bar)
>             ^^^^^
>      found: #Bad{foo: _}
>   expected: {bar: _}
> This #Bad{foo: _} comes from test:5:
>       #Bad{foo: x.foo}
>       ^^^^^^^^^^^^^^^^
> but is used as {bar: _} at test:10:
>     (y.foo, y.bar)
>              ^^^^

let make = fn (x) {
  if x.good {
    #Good{foo: x.foo, bar: x.bar}
  } else {
    #Bad{foo: x.foo}
  }
};
let use = fn (y) {
  match y {
  | #Good{foo: bar, bar: z} => bar
  | #Bad{foo: bar} => bar
  }
};
fn (x) { use(make(x)) }
> let make =
> fn (x) { if x.good { #Good{foo: x.foo, bar: x.bar} } else { #Bad{foo: x.foo} } };
> let use =
> fn (y) { match y { #Good{foo: bar, bar: z} => bar | #Bad{foo: bar} => bar } };
> fn (x) {
>   use(make(x))
> }
>   let make :
>     [a, b] ({good: Bool, foo: b, bar: a}) ->
>     #Good{foo: b, bar: a} | #Bad{foo: b} =
>   fn [a, b](x: {good: Bool, foo: b, bar: a}) ->
>     (#Good{foo: b, bar: a} | #Bad{foo: b}) {
>     if x.good {
>       #Good{foo: x.foo, bar: x.bar}
>     } else {
>       #Bad{foo: x.foo}
>     }
>   };
>   let use : [a, b] (#Good{foo: a, bar: Any} | #Bad{foo: b}) -> a | b =
>   fn [a, b](y: #Good{foo: a, bar: Any} | #Bad{foo: b}) -> (a | b) {
>     match y {
>       | #Good{foo: bar, bar: z} => bar
>       | #Bad{foo: bar} => bar
>     }
>   };
>   fn [a](x: {good: Bool, foo: a, bar: Any}) -> a {
>     use(make(x))
>   }
> [a] ({good: Bool, foo: a, bar: Any}) -> a
> let make =
>   fun (x) ret {
>     match x.good {
>       true -> {
>         ret(Good(foo: x.foo, bar: x.bar))
>       };
>       false -> {
>         ret(Bad(foo: x.foo))
>       }
>     }
>   };
> let use =
>   fun (y) ret {
>     match y {
>       Good -> {
>         let bar = y.foo;
>         let z = y.bar;
>         ret(bar)
>       };
>       Bad -> {
>         let bar = y.foo;
>         ret(bar)
>       }
>     }
>   };
> let  = %yield(fun (x) ret { let x2 = make(x); let x3 = use(x2); ret(x3) });
> done


fn(a : (#T|#F),b : #T|#F,c : #T|#F) {
  match (a, b, c) {
  | (#T, #T, _)
  | (_, #T, #T)
  | (#T, _, #T) => true

  | (#F, #F, _)
  | (_, #F, #F)
  | (#F, _, #F) => false
  }
}
> fn (a: #T | #F, b: #T | #F, c: #T | #F) {
>   match (a, b, c) {
>     | (#T, #T, _)
>     | (_, #T, #T)
>     | (#T, _, #T) => true
>     | (#F, #F, _)
>     | (_, #F, #F)
>     | (#F, _, #F) => false
>   }
> }
>   fn (a: #T | #F, b: #T | #F, c: #T | #F) -> Bool {
>     match (a, b, c) {
>       | (#T, #T, _)
>       | (_, #T, #T)
>       | (#T, _, #T) => true
>       | (#F, #F, _)
>       | (_, #F, #F)
>       | (#F, _, #F) => false
>     }
>   }
> (#T | #F, #T | #F, #T | #F) -> Bool
> let  = %yield(fun (a, b, c) ret {
>   match (0: a, 1: b, 2: c).0 {
>     T -> {
>       match (0: a, 1: b, 2: c).1 {
>         T -> {
>           ret(<true>)
>         };
>         F -> {
>           match (0: a, 1: b, 2: c).2 {
>             T -> {
>               ret(<true>)
>             };
>             F -> {
>               ret(<false>)
>             }
>           }
>         }
>       }
>     };
>     F -> {
>       match (0: a, 1: b, 2: c).1 {
>         T -> {
>           match (0: a, 1: b, 2: c).2 {
>             T -> {
>               ret(<true>)
>             };
>             F -> {
>               ret(<false>)
>             }
>           }
>         };
>         F -> {
>           ret(<false>)
>         }
>       }
>     }
>   }
> });
> done

fn(x : #{a?: (), b?: (), c?: ()}) {
  match x {
  | #{a?:(), b?:(), _}
  | #{b?:(), c?:(), _}
  | #{c?:(), a?:(), _} => true

  | #{a?:absent, b?:absent, _}
  | #{b?:absent, c?:absent, _}
  | #{c?:absent, a?:absent, _} => false
  }
}
> fn (x: #{a?: (), b?: (), c?: ()}) {
>   match x {
>     | #{a?: (), b?: (), _}
>     | #{b?: (), c?: (), _}
>     | #{c?: (), a?: (), _} => true
>     | #{a?: absent, b?: absent, _}
>     | #{b?: absent, c?: absent, _}
>     | #{c?: absent, a?: absent, _} => false
>   }
> }
>   fn (x: #{a?: (), b?: (), c?: ()}) -> Bool {
>     match x {
>       | #{a?: (), b?: (), _}
>       | #{b?: (), c?: (), _}
>       | #{c?: (), a?: (), _} => true
>       | #{a?: absent, b?: absent, _}
>       | #{b?: absent, c?: absent, _}
>       | #{c?: absent, a?: absent, _} => false
>     }
>   }
> (#{a?: (), b?: (), c?: ()}) -> Bool
> let  = %yield(fun (x) ret {
>   ifpresent x.c {
>     ifpresent x.b {
>       ifpresent x.a {
>         ret(<true>)
>       } else {
>         ret(<true>)
>       }
>     } else {
>       ifpresent x.a {
>         ret(<true>)
>       } else {
>         ret(<false>)
>       }
>     }
>   } else {
>     ifpresent x.b {
>       ifpresent x.a {
>         ret(<true>)
>       } else {
>         ret(<false>)
>       }
>     } else {
>       ret(<false>)
>     }
>   }
> });
> done


fn(a : (#T|#F),b : #T|#F,c : #T|#F) {
  match (a, b, c) {
  | (#T, #T, _)
  | (#T, _, #T) => true

  | (_, #F, #F)
  | (#F, _, #F) => false
  }
}
> fn (a: #T | #F, b: #T | #F, c: #T | #F) {
>   match (a, b, c) {
>     | (#T, #T, _)
>     | (#T, _, #T) => true
>     | (_, #F, #F)
>     | (#F, _, #F) => false
>   }
> }
> test:2: Some cases of this pattern-match are missing:
>     match (a, b, c) {
>           ^^^^^^^^^
> The following cases are unhandled:
> | (#F, _, #T)
>   fn (a: #T | #F, b: #T | #F, c: #T | #F) -> Bool {
>     match (a, b, c) {
>       | (#T, #T, _)
>       | (#T, _, #T) => true
>       | (_, #F, #F)
>       | (#F, _, #F) => false
>     }
>   }
> (#T | #F, #T | #F, #T | #F) -> Bool
> let  = %yield(fun (a, b, c) ret {
>   match (0: a, 1: b, 2: c).0 {
>     T -> {
>       match (0: a, 1: b, 2: c).1 {
>         T -> {
>           ret(<true>)
>         };
>         F -> {
>           match (0: a, 1: b, 2: c).2 {
>             T -> {
>               ret(<true>)
>             };
>             F -> {
>               ret(<false>)
>             }
>           }
>         }
>       }
>     };
>     F -> {
>       match (0: a, 1: b, 2: c).2 {
>         F -> {
>           ret(<false>)
>         };
>         T -> {
>           match failure
>         }
>       }
>     }
>   }
> });
> done

fn(x : (#A|#B|#C), y : (#A|#B|#C)) {
  match x, y {
    #A, #A => 1
  }
}
> fn (x: #A | #B | #C, y: #A | #B | #C) { match x, y { #A, #A => 1 } }
> test:2: Some cases of this pattern-match are missing:
>     match x, y {
>           ^^^^
> The following cases are unhandled:
> | #A, (#B | #C)
> | (#B | #C), _
>   fn (x: #A | #B | #C, y: #A | #B | #C) -> Int { match x, y { #A, #A => 1 } }
> (#A | #B | #C, #A | #B | #C) -> Int
> let  = %yield(fun (x, y) ret {
>   match x {
>     A -> {
>       match y {
>         A -> {
>           ret(1)
>         };
>         (B|C) -> {
>           match failure
>         }
>       }
>     };
>     (B|C) -> {
>       match failure
>     }
>   }
> });
> done

fn (x : ((#A(Int,Int)|#B{x:String,y:String}|#C|#D|#E), #B|#C|#D)) {
  match x {
  | (#A(_,_)|#B{}, #C|#D)
  | (#C, #C)
  | (#D, #C) => 1
  }
}
> fn (x: (#A(Int, Int) | #B{x: String, y: String} | #C | #D | #E, #B | #C | #D)) {
>   match x {
>     | ((#A(_, _) | #B{}), (#C | #D))
>     | (#C, #C)
>     | (#D, #C) => 1
>   }
> }
> test:3: A surplus field 'y' is present.
>     | (#A(_,_)|#B{}, #C|#D)
>                ^^^^
>      found: #B{x: _, y: _}
>   expected: #B
> This #B{x: _, y: _} comes from test:1:
>   fn (x : ((#A(Int,Int)|#B{x:String,y:String}|#C|#D|#E), #B|#C|#D)) {
>                                     ^
> but is used as #B

fn(x : #A(Int, #P|#Q|#R) | #B(Int, #P|#Q|#R)) {
  match #{averylongnameinanexpression: #{averylongnameinanexpression: x}}.averylongnameinanexpression.averylongnameinanexpression, #{x:0,y:5}.y {
  | #A(x, #P|#Q), y => 1
  | #B(x, #P|#Q), y => 1
  }
}
> fn (x: #A(Int, #P | #Q | #R) | #B(Int, #P | #Q | #R)) {
>   match
>     #{averylongnameinanexpression: #{averylongnameinanexpression: x}}.averylongnameinanexpression.averylongnameinanexpression,
>     #{x: 0, y: 5}.y
>   {
>     | #A(x, (#P | #Q)), y => 1
>     | #B(x, (#P | #Q)), y => 1
>   }
> }
> test:2: Some cases of this pattern-match are missing:
>     match #{averylongnameinanexpression: #{averylongnameinanexpression: x}}.averylongnameinanexpression.averylongnameinanexpression, #{x:0,y:5}.y {
>           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
> The following cases are unhandled:
> | #A(_, #R), _
> | #B(_, #R), _
>   fn (x: #A(Int, #P | #Q | #R) | #B(Int, #P | #Q | #R)) -> Int {
>     match
>       #{averylongnameinanexpression: #{averylongnameinanexpression: x}}.averylongnameinanexpression.averylongnameinanexpression,
>       #{x: 0, y: 5}.y
>     {
>       | #A(x, (#P | #Q)), y => 1
>       | #B(x, (#P | #Q)), y => 1
>     }
>   }
> (#A(Int, #P | #Q | #R) | #B(Int, #P | #Q | #R)) -> Int
> let  = %yield(fun (x) ret {
>   match (averylongnameinanexpression: (averylongnameinanexpression: x)).averylongnameinanexpression.averylongnameinanexpression {
>     A -> {
>       let x2 =
>         (averylongnameinanexpression: (averylongnameinanexpression: x)).averylongnameinanexpression.averylongnameinanexpression.0;
>       match (averylongnameinanexpression: (averylongnameinanexpression: x)).averylongnameinanexpression.averylongnameinanexpression.1 {
>         P -> {
>           let y = (x: 0, y: 5).y;
>           ret(1)
>         };
>         Q -> {
>           let y = (x: 0, y: 5).y;
>           ret(1)
>         };
>         R -> {
>           match failure
>         }
>       }
>     };
>     B -> {
>       let x2 =
>         (averylongnameinanexpression: (averylongnameinanexpression: x)).averylongnameinanexpression.averylongnameinanexpression.0;
>       match (averylongnameinanexpression: (averylongnameinanexpression: x)).averylongnameinanexpression.averylongnameinanexpression.1 {
>         P -> {
>           let y = (x: 0, y: 5).y;
>           ret(1)
>         };
>         Q -> {
>           let y = (x: 0, y: 5).y;
>           ret(1)
>         };
>         R -> {
>           match failure
>         }
>       }
>     }
>   }
> });
> done

let calls_any = fn(f) {
  (f(#Foo), f(#Bar), f(#Baz))
};
let use = fn(x) {
  match x {
  | #Foo => 1
  | #Bar => 2
  }
};
calls_any(use)
> let calls_any = fn (f) { (f(#Foo), f(#Bar), f(#Baz)) };
> let use = fn (x) { match x { #Foo => 1 | #Bar => 2 } };
> calls_any(use)
> test:10: The tag should be #Foo|#Bar, but tag #Baz is present.
>   calls_any(use)
>             ^^^
>      found: (#Foo | #Bar) -> _
>   expected: #Baz -> _
> This #Baz comes from test:2:
>     (f(#Foo), f(#Bar), f(#Baz))
>                          ^^^^
> but is used as #Foo | #Bar at test:5:
>     match x {
>           ^

# codegen test for single-case matches
fn(x) {
  match x {
  | (a, b, c) =>
    let c2 = match c { y => y };
    (c2, b, a)
  }
}
> fn (x) { match x { (a, b, c) => (let c2 = match c { y => y }; (c2, b, a)) } }
>   fn [a, b, c](x: (a, b, c)) -> (c, b, a) {
>     match x {
>       | (a, b, c) => (let c2 : c = match c { y => y }; (c2, b, a))
>     }
>   }
> [a, b, c] (a, b, c) -> (c, b, a)
> let  = %yield(fun (x) ret {
>   let a = x.0;
>   let b = x.1;
>   let c = x.2;
>   ret((0: c, 1: b, 2: a))
> });
> done

# tricky binding
fn(x) {
  match x {
  | (a, b, #T) | (b, a, #F) => (a,b)
  }
}
> fn (x) { match x { (a, b, #T) | (b, a, #F) => (a, b) } }
>   fn [a, b](x: (a, b, #T | #F)) -> (a | b, a | b) {
>     match x {
>       | (a, b, #T)
>       | (b, a, #F) => (a, b)
>     }
>   }
> [a, b] (a, b, #T | #F) -> (a | b, a | b)
> let  = %yield(fun (x) ret {
>   let case(a, b) = {
>     ret((0: a, 1: b))
>   };
>   let a = x.0;
>   let b = x.1;
>   match x.2 {
>     T -> {
>       case(a, b)
>     };
>     F -> {
>       case(b, a)
>     }
>   }
> });
> done

fn(x, y) {
  match x, y {
  | #A, #A => 1
  | #A, #B => 2
  | #B, #A => 3
  }
}
> fn (x, y) { match x, y { #A, #A => 1 | #A, #B => 2 | #B, #A => 3 } }
>   fn (x: #A | #B, y: #A) -> Int {
>     match x, y {
>       | #A, #A => 1
>       | #A, #B => 2
>       | #B, #A => 3
>     }
>   }
> (#A | #B, #A) -> Int
> let  = %yield(fun (x, y) ret {
>   match x {
>     A -> {
>       match y {
>         A -> {
>           ret(1)
>         };
>         B -> {
>           ret(2)
>         }
>       }
>     };
>     B -> {
>       ret(3)
>     }
>   }
> });
> done

# Compilation & errors with default cases
fn (x : #A | #B | #C | #D | #E, y) -> Int {
  match x, y {
  | #A, (#A | #B) => 1
  | _, (#A | #B) => 2
  }
}
> fn (x: #A | #B | #C | #D | #E, y) -> Int {
>   match x, y {
>     | #A, (#A | #B) => 1
>     | _, (#A | #B) => 2
>   }
> }
>   fn (x: #A | #B | #C | #D | #E, y: #A | #B) -> Int {
>     match x, y {
>       | #A, (#A | #B) => 1
>       | _, (#A | #B) => 2
>     }
>   }
> (#A | #B | #C | #D | #E, #A | #B) -> Int
> let  = %yield(fun (x, y) ret {
>   match x {
>     A -> {
>       ret(1)
>     };
>     (B|C|D|E) -> {
>       ret(2)
>     }
>   }
> });
> done

fn (x : #A | #B | #C | #D | #E, y: #A | #B) -> Int {
  match x, y {
  | #A, (#A | #B) => 1
  | _, (#A) => 2
  }
}
> fn (x: #A | #B | #C | #D | #E, y: #A | #B) -> Int {
>   match x, y {
>     | #A, (#A | #B) => 1
>     | _, #A => 2
>   }
> }
> test:2: Some cases of this pattern-match are missing:
>     match x, y {
>           ^^^^
> The following cases are unhandled:
> | (#B | #C | #D | #E), #B
>   fn (x: #A | #B | #C | #D | #E, y: #A | #B) -> Int {
>     match x, y {
>       | #A, (#A | #B) => 1
>       | _, #A => 2
>     }
>   }
> (#A | #B | #C | #D | #E, #A | #B) -> Int
> let  = %yield(fun (x, y) ret {
>   match x {
>     A -> {
>       ret(1)
>     };
>     (B|C|D|E) -> {
>       match y {
>         A -> {
>           ret(2)
>         };
>         B -> {
>           match failure
>         }
>       }
>     }
>   }
> });
> done


fn (x) {
  match x {
  | #{foo:#A, _} => 1
  | #{foo:#B, bar:n} => n
  }
}
> fn (x) { match x { #{foo: #A, _} => 1 | #{foo: #B, bar: n} => n } }
>   fn [a](x: #{foo: #A | #B, bar: a}) -> (Int | a) {
>     match x {
>       | #{foo: #A, _} => 1
>       | #{foo: #B, bar: n} => n
>     }
>   }
> [a] (#{foo: #A | #B, bar: a}) -> Int | a
> let  = %yield(fun (x) ret {
>   match x.foo {
>     A -> {
>       ret(1)
>     };
>     B -> {
>       let n = x.bar;
>       ret(n)
>     }
>   }
> });
> done

fn (x) {
  match x {
  | #{foo:#A, _} => 1
  | #{foo:#B, _} => n
  }
}
> fn (x) { match x { #{foo: #A, _} => 1 | #{foo: #B, _} => n } }
> test:3: Cannot determine which fields this pattern matches
>     | #{foo:#A, _} => 1
>       ^^^^^^^^^^^^

fn (x:#A|#B,y:#P(Int) | #Q(Int)) {
  match x,y {
  | #A, #P(x) => x
  | #A, #Q(x) => x
  | #B, #P(x) => x
  }
}
> fn (x: #A | #B, y: #P(Int) | #Q(Int)) {
>   match x, y {
>     | #A, #P(x) => x
>     | #A, #Q(x) => x
>     | #B, #P(x) => x
>   }
> }
> test:2: Some cases of this pattern-match are missing:
>     match x,y {
>           ^^^
> The following cases are unhandled:
> | #B, #Q(_)
>   fn (x: #A | #B, y: #P(Int) | #Q(Int)) -> Int {
>     match x, y {
>       | #A, #P(x) => x
>       | #A, #Q(x) => x
>       | #B, #P(x) => x
>     }
>   }
> (#A | #B, #P(Int) | #Q(Int)) -> Int
> let  = %yield(fun (x, y) ret {
>   match x {
>     A -> {
>       match y {
>         P -> {
>           let x2 = y.0;
>           ret(x2)
>         };
>         Q -> {
>           let x2 = y.0;
>           ret(x2)
>         }
>       }
>     };
>     B -> {
>       match y {
>         P -> {
>           let x2 = y.0;
>           ret(x2)
>         };
>         Q -> {
>           match failure
>         }
>       }
>     }
>   }
> });
> done

fn(x) {
  match x {
  | #{foo?: x, bar?: y, baz:z} => (x,y,z)
  | #{foo?: absent, baz, _}
  | #{bar?: absent, baz, _} => (0,0,baz)
  }
}
> fn (x) {
>   match x {
>     | #{foo?: x, bar?: y, baz: z} => (x, y, z)
>     | #{foo?: absent, baz, _}
>     | #{bar?: absent, baz, _} => (0, 0, baz)
>   }
> }
>   fn [a, b, c](x: #{foo?: c, bar?: a, baz: b}) -> (Int | c, Int | a, b) {
>     match x {
>       | #{foo?: x, bar?: y, baz: z} => (x, y, z)
>       | #{foo?: absent, baz, _}
>       | #{bar?: absent, baz, _} => (0, 0, baz)
>     }
>   }
> [a, b, c] (#{foo?: c, bar?: a, baz: b}) -> (Int | c, Int | a, b)
> let  = %yield(fun (x) ret {
>   let case(baz) = {
>     ret((0: 0, 1: 0, 2: baz))
>   };
>   ifpresent x.bar {
>     ifpresent x.foo {
>       let x2 = x.foo;
>       let y = x.bar;
>       let z = x.baz;
>       ret((0: x2, 1: y, 2: z))
>     } else {
>       let baz = x.baz;
>       case(baz)
>     }
>   } else {
>     let baz = x.baz;
>     case(baz)
>   }
> });
> done

# unreachable patterns
fn (x : #A(Int) | #B(Int)) {
  match x {
  | #A(_) | #B(_) => 1
  | #A(a) => a
  }
}
> fn (x: #A(Int) | #B(Int)) { match x { #A(_) | #B(_) => 1 | #A(a) => a } }
> test:4: This pattern is unused
>     | #A(a) => a
>       ^^^^^
>   fn (x: #A(Int) | #B(Int)) -> Int {
>     match x {
>       | #A(_)
>       | #B(_) => 1
>       | #A(a) => a
>     }
>   }
> (#A(Int) | #B(Int)) -> Int
> let  = %yield(fun (x) ret {
>   match x {
>     A -> {
>       let a = x.0;
>       ret(1)
>     };
>     B -> {
>       ret(1)
>     }
>   }
> });
> done

# partially unused patterns
fn (x: (#A|#B|#C, Int)) {
  match x {
  | (#A | #B, a) => a
  | ((#B | #C), _) => 1
  }
}
> fn (x: (#A | #B | #C, Int)) {
>   match x {
>     | ((#A | #B), a) => a
>     | ((#B | #C), _) => 1
>   }
> }
> test:4: This pattern is unused
>     | ((#B | #C), _) => 1
>         ^^
>   fn (x: (#A | #B | #C, Int)) -> Int {
>     match x {
>       | ((#A | #B), a) => a
>       | ((#B | #C), _) => 1
>     }
>   }
> (#A | #B | #C, Int) -> Int
> let  = %yield(fun (x) ret {
>   match x.0 {
>     A -> {
>       let a = x.1;
>       ret(a)
>     };
>     B -> {
>       let a = x.1;
>       ret(a)
>     };
>     C -> {
>       ret(1)
>     }
>   }
> });
> done

> STATS: fix: 16, flex: 208
