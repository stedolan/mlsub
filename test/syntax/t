let f = fn (x) { x, x }; f
> let f = fn (x) { (<err>) }; f
> test:1: syntax error
>   let f = fn (x) { x, x }; f
>                     ^

let x = 1; (let y = 2 2; {x,y})
> let x = 1; (<err>)
> test:1: syntax error
>   let x = 1; (let y = 2 2; {x,y})
>                         ^

fn (id : [a]. a -> a) { id(42) }
> fn (<err>) { id(42) }
> test:1: syntax error
>   fn (id : [a]. a -> a) { id(42) }
>               ^

fn (x)
> parser failure: bad parse


# paren insertion
fn(a) { { foo: if true { fn(x) { x } } else { a } } }
> fn (a) { {foo: if true { fn (x) { x } } else { a }} }
>   fn [a, b](a: a) -> ({foo: (b -> b) | a}) {
>     {foo: if true { fn [a_2](x: a_2) -> a_2 { x } } else { a }}
>   }
> [a, b] a -> {foo: (b -> b) | a}
> let  = %yield(fun (a) ret {
>   let k(x) = {
>     ret((foo: x))
>   };
>   match <true> {
>     true() -> {
>       k(fun (x) ret2 { ret2(x) })
>     };
>     false() -> {
>       k(a)
>     }
>   }
> });
> done

fn[a,b](x:a) -> (a | a -> b) { x }
> fn [a, b](x: a) -> (a | (a -> b)) { x }
>   fn [a, b](x: a) -> ((a -> b) | a) { x }
> [a, b] a -> (a -> b) | a
> let  = %yield(fun (x) ret { ret(x) }); done

: [a <: ([] Int) -> Int ] a <: Int
> test:1: Bounds must be simple types
>   : [a <: ([] Int) -> Int ] a <: Int
>           ^^^^^^^^^^^^^^^
> STATS: fix: 3, flex: 59
